name: Global Travel Monitor Schedule

on:
  schedule:
    # 매일 UTC 00:00 (한국 09:00 / 캘리포니아 16:00)
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-global-monitor:
    runs-on: ubuntu-latest
    
    env:
      R_LIBS_USER: ./R/library
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}

    steps:
      - uses: actions/checkout@v3
      
      - uses: r-lib/actions/setup-r@v2
      
      - uses: r-lib/actions/setup-pandoc@v2
      
      - name: Install Linux Deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libssl-dev libxml2-dev libfontconfig1-dev libharfbuzz-dev libfribidi-dev libfreetype6-dev libpng-dev libtiff5-dev libjpeg-dev
        
      - name: Cache R Packages
        uses: actions/cache@v3
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ hashFiles('.github/workflows/*.yml') }}
          restore-keys: |
            ${{ runner.os }}-r-

      - name: Install R Packages (Fast RSPM)
        run: |
          dir.create(Sys.getenv("R_LIBS_USER"), recursive = TRUE, showWarnings = FALSE)
          
          # [핵심 수정] repos 주소를 'RSPM (Linux Binary)'으로 변경
          # 이렇게 하면 컴파일 과정이 없어서 에러가 안 나고 속도가 엄청 빠릅니다.
          install.packages(c("rmarkdown", "pacman", "httr", "rvest", "tidyverse", "telegram.bot", "lubridate", "stringr", "readr"), 
                           repos = "https://packagemanager.posit.co/cran/__linux__/jammy/latest", 
                           lib = Sys.getenv("R_LIBS_USER"))
        shell: Rscript {0}
        
      - name: Run Global Monitor
        run: Rscript -e "rmarkdown::render('US_global_travel_monitor.Rmd')"
      
      - name: Commit and Push Data
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          if [ -f travel_status_memory.csv ]; then
            echo "✅ CSV file found. Committing..."
            git add travel_status_memory.csv
            git commit -m "Update travel advisory data" || echo "No changes to commit"
            git push "https://${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main
          else
            echo "⚠️ CSV file was NOT created. Skipping commit."
          fi
