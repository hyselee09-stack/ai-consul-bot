---
title: "Consular Protection Monitor"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

# 1. 라이브러리 로드 (설치 안 된 경우 자동 설치)
if (!require("pacman")) install.packages("pacman")
pacman::p_load(httr, jsonlite, tidyRSS, tidyverse, telegram.bot, rvest, lubridate, purrr)

# ==============================================================================
# 2. 보안 설정: GitHub Secrets에서 비밀번호 가져오기
# (주의: 이 파일에 절대 진짜 키를 적지 마세요!)
# ==============================================================================
GEMINI_API_KEY     <- Sys.getenv("GEMINI_API_KEY")
TELEGRAM_BOT_TOKEN <- Sys.getenv("TELEGRAM_BOT_TOKEN")
MY_CHAT_ID         <- Sys.getenv("MY_CHAT_ID")

# [설정] 사용할 모델명 (요청하신 2.5 Flash)
MODEL_NAME         <- "gemini-2.5-flash"

# 텔레그램 봇 초기화
bot <- Bot(token = TELEGRAM_BOT_TOKEN)

# ==============================================================================
# 3. 핵심 함수 정의
# ==============================================================================

# [함수 1] 제미나이 직접 호출 (수동 API 요청)
ask_gemini_direct <- function(prompt) {
  url <- paste0("https://generativelanguage.googleapis.com/v1beta/models/", MODEL_NAME, ":generateContent?key=", GEMINI_API_KEY)
  
  body_data <- list(
    contents = list(list(parts = list(list(text = prompt))))
  )
  
  tryCatch({
    response <- POST(
      url, 
      add_headers("Content-Type" = "application/json"), 
      body = toJSON(body_data, auto_unbox = TRUE)
    )
    
    if (status_code(response) == 200) {
      result_json <- fromJSON(content(response, "text", encoding = "UTF-8"))
      return(result_json$candidates$content$parts[[1]]$text)
    } else {
      print(paste("⚠️ API Error Code:", status_code(response)))
      return(NULL)
    }
  }, error = function(e) {
    print("⚠️ 제미나이 통신 실패")
    return(NULL)
  })
}

# [함수 2] 뉴스 본문 스크래핑 (제목만 보는 한계 극복)
get_full_text <- function(link) {
  tryCatch({
    # 타임아웃 옵션 추가하여 멈춤 방지
    page <- read_html(link, options = "NO_ERROR")
    # <p> 태그만 긁어오기
    text <- page %>% html_nodes("p") %>% html_text() %>% paste(collapse = " ")
    
    # 너무 짧으면(봇 차단 등) NULL 반환, 길면 2000자만 자름
    if (nchar(text) < 100) return(NULL)
    return(substr(text, 1, 2000))
  }, error = function(e) {
    return(NULL) 
  })
}

# [함수 3] 뉴스 수집 (영어 + 한국어 4대 소스)
fetch_news <- function() {
  # 1. Google News US (영어)
  url_google_us <- "https://news.google.com/rss/search?q=California+(accident+OR+disaster+OR+shooting+OR+crash)+when:1h&ceid=US:en&hl=en-US&gl=US"
  # 2. 미주 중앙일보 (LA 사회)
  url_joongang <- "https://rss.koreadaily.com/la/society.xml"
  # 3. 라디오코리아 (로컬 뉴스)
  url_radiokorea <- "https://www.radiokorea.com/rss/news_news.xml"
  # 4. 미주 한국일보 (구글 우회)
  url_hankook <- "https://news.google.com/rss/search?q=site:koreatimes.com+California+(사건+OR+사고+OR+화재+OR+총격)+when:1h&ceid=KR:ko&hl=ko&gl=KR"

  safe_fetch <- function(url, src) {
    tryCatch({
      feed <- tidyRSS::tidyfeed(url)
      if (nrow(feed) > 0) {
        feed %>% 
          select(item_title, item_link, item_description, item_pub_date) %>% 
          mutate(source = src) %>% 
          head(5) # 소스당 최신 5개
      } else { NULL }
    }, error = function(e) { return(NULL) })
  }
  
  bind_rows(
    safe_fetch(url_google_us, "Google_US"),
    safe_fetch(url_joongang, "Joongang_Ilbo"),
    safe_fetch(url_radiokorea, "Radio_Korea"),
    safe_fetch(url_hankook, "Hankook_Ilbo")
  )
}



# ==============================================================================
# 4. 메인 실행 로직 (생존 신고 기능 추가됨)
# ==============================================================================
print(paste("📡 [GitHub Action]", Sys.time(), "뉴스 체크 시작 (Model:", MODEL_NAME, ")"))

news_data <- fetch_news()

# 알림 보낸 횟수 카운트 (초기화)
alert_count <- 0

if (is.null(news_data) || nrow(news_data) == 0) {
  print("   -> 수집된 뉴스가 없습니다.")
  # 뉴스 자체가 없어도 생존 신고 발송
  try(bot$sendMessage(chat_id = MY_CHAT_ID, text = "✅ [점검] 수집된 새 뉴스가 없습니다. (시스템 정상)"))
  
} else {
  
  # [중요] 최근 40분 이내 기사만 필터링
  current_time <- Sys.time()
  news_data <- news_data %>%
    mutate(pub_date_parsed = ymd_hms(item_pub_date)) %>%
    filter(pub_date_parsed >= (current_time - minutes(40)))
  
  if (nrow(news_data) > 0) {
    print(paste("   ->", nrow(news_data), "개의 최신 기사를 분석합니다."))
    
    for(i in 1:nrow(news_data)) {
      title <- news_data$item_title[i]; link <- news_data$item_link[i]; desc <- news_data$item_description[i]; src <- news_data$source[i]
      
      # 본문 긁기 시도
      full_text <- get_full_text(link)
      content_src <- if(is.null(full_text)) gsub("<.*?>", "", desc) else full_text
      
      # 프롬프트
      prompt <- paste0(
        "Analyze this news.\n Source: ", src, "\n Title: ", title, "\n Content: ", content_src, "\n\n",
        "TASK: Identify negative incidents (crime, accident, disaster) involving Koreans in California.\n",
        "CRITERIA:\n 1. Korean Media: Check if negative incident.\n 2. Google US: Check names/locations/businesses.\n",
        "OUTPUT: 'Major: [Yes/No] | Korean: [Yes/No] | Reason: [Short logic] | Summary: [1 sentence Korean]'"
      )
      
      Sys.sleep(4)
      analysis <- ask_gemini_direct(prompt)
      
      # 분석 결과 처리
      if (!is.null(analysis) && str_detect(analysis, "Korean: Yes")) {
        summary <- str_extract(analysis, "(?<=Summary: ).*")
        reason <- str_extract(analysis, "(?<=Reason: ).*(?=\\| Summary)")
        msg <- paste0("🚨 [긴급] 한인 관련 사건!\n\n📰 ", src, "\n📌 ", title, "\n🧐 ", reason, "\n📝 ", summary, "\n🔗 ", link)
        
        tryCatch({
          bot$sendMessage(chat_id = MY_CHAT_ID, text = msg)
          print(paste("✅ 알림 전송:", title))
          alert_count <- alert_count + 1  # 카운트 증가
        }, error = function(e) { print("전송 실패") })
      }
    }
    
    # [추가된 로직] 루프가 다 끝났는데 보낸 알림이 0개라면? -> '이상 없음' 보고
    if (alert_count == 0) {
      safe_msg <- paste0("✅ [점검] 최근 40분간 ", nrow(news_data), "건의 뉴스를 확인했으나, 한인 관련 특이사항이 없습니다.")
      try(bot$sendMessage(chat_id = MY_CHAT_ID, text = safe_msg))
    }

  } else {
    print("   -> 최근 40분 내 새로운 기사가 없습니다.")
    # 기사는 없지만 코드는 돌았다는 생존 신고
    try(bot$sendMessage(chat_id = MY_CHAT_ID, text = "✅ [점검] 최근 40분 내 발생한 새 뉴스가 없습니다."))
  }
}
